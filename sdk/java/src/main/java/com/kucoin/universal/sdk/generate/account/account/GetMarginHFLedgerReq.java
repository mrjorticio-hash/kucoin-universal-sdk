// Code generated by Kucoin Universal SDK Generator; DO NOT EDIT.

package com.kucoin.universal.sdk.generate.account.account;

import com.fasterxml.jackson.annotation.JsonCreator;
import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.fasterxml.jackson.annotation.JsonValue;
import com.kucoin.universal.sdk.internal.interfaces.Request;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;

@AllArgsConstructor
@Builder
@Data
@NoArgsConstructor
@JsonIgnoreProperties(ignoreUnknown = true)
public class GetMarginHFLedgerReq implements Request {
  /**
   * Currency optional; more than one can be selected; separate using commas; select no more than 10
   * currencies; the default will be to query for all currencies if left empty
   */
  @JsonProperty("currency")
  private String currency;

  /** direction: in, out */
  @JsonProperty("direction")
  private DirectionEnum direction;

  /**
   * Transaction type: TRANSFER- transfer funds, MARGIN_EXCHANGE-cross margin trade,
   * ISOLATED_EXCHANGE-isolated margin trade, LIQUIDATION-liquidation, ASSERT_RETURN-forced
   * liquidation asset return
   */
  @JsonProperty("bizType")
  private String bizType;

  /**
   * The ID of the last set of data from the previous data batch. By default, the latest information
   * is given.
   */
  @JsonProperty("lastId")
  private Long lastId;

  /** Default100, Max200 */
  @JsonProperty("limit")
  @Builder.Default
  private Integer limit = 100;

  /** Start time (milliseconds) */
  @JsonProperty("startAt")
  private Long startAt;

  /** End time (milliseconds) */
  @JsonProperty("endAt")
  private Long endAt;

  public enum DirectionEnum {
    /** */
    IN("in"),
    /** */
    OUT("out");

    private final String value;

    DirectionEnum(String value) {
      this.value = value;
    }

    @JsonValue
    public String getValue() {
      return value;
    }

    @Override
    public String toString() {
      return String.valueOf(value);
    }

    @JsonCreator
    public static DirectionEnum fromValue(String value) {
      for (DirectionEnum b : DirectionEnum.values()) {
        if (b.value.equals(value)) {
          return b;
        }
      }
      throw new IllegalArgumentException("Unexpected value '" + value + "'");
    }
  }
}
